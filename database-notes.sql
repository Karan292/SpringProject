-- Optional manual database creation.
-- Tables are created automatically by Hibernate when the app starts.

CREATE DATABASE IF NOT EXISTS campus_placement_db;
USE campus_placement_db;

-- After running the application, Hibernate will create tables like:
-- students
-- companies
-- job_opportunities
-- job_applications
